/**
 * Sprint-04 spec 命名别名 — 等同 `NovaLedAdapter` (诺瓦 VX1000 + Intel NUC 播控)
 * 此文件提供 spec 期望的 `real-led.adapter.ts` 路径与 `RealLedAdapter` 类名。
 */
export { NovaLedAdapter as RealLedAdapter } from './nova-led.adapter';
