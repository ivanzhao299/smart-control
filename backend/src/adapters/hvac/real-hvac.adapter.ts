/**
 * Sprint-04 spec 命名别名 — 等同 `ModbusHvacAdapter` (奥克斯中央空调 + Modbus TCP)
 * 此文件提供 spec 期望的 `real-hvac.adapter.ts` 路径与 `RealHvacAdapter` 类名。
 */
export { ModbusHvacAdapter as RealHvacAdapter } from './modbus-hvac.adapter';
