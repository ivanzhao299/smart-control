/**
 * Sprint-03 spec 命名别名 — Power 当前只有 mock 实现, `PowerAdapter` 即 mock 行为。
 * 此文件提供 spec 期望的 `mock-power.adapter.ts` 路径与 `MockPowerAdapter` 类名。
 */
export {
  PowerAdapter as MockPowerAdapter,
  type PowerState,
} from './power.adapter';
